package com.ricdip.interpreters.diceroller.parser.ast;

/**
 * Interface implemented by each AST node that is also an expression (a node that produces a value).
 */
public interface Expression extends Node {
}
